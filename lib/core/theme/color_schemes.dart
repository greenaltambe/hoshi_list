import 'package:flutter/material.dart';

// Fallback color schemes in case dynamic theming is unavailable
final lightFallbackColorScheme = ColorScheme.fromSeed(
  seedColor: Colors.deepPurple,
  brightness: Brightness.light,
);

// Fallback dark color scheme
final darkFallbackColorScheme = ColorScheme.fromSeed(
  seedColor: Colors.deepPurple,
  brightness: Brightness.dark,
);
